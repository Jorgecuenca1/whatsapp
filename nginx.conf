events {
    worker_connections 1024;
}

http {
    upstream whatsapp-bot {
        server whatsapp-bot:3002;
    }

    server {
        listen 80;
        server_name tu-dominio.com;  # Cambia esto por tu dominio

        # Redirigir a HTTPS (descomenta cuando tengas SSL)
        # return 301 https://$server_name$request_uri;
        
        location / {
            proxy_pass http://whatsapp-bot;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_cache_bypass $http_upgrade;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }

    # Configuración SSL (descomenta cuando tengas certificados)
    # server {
    #     listen 443 ssl;
    #     server_name tu-dominio.com;
    #
    #     ssl_certificate /etc/nginx/ssl/cert.pem;
    #     ssl_certificate_key /etc/nginx/ssl/key.pem;
    #
    #     location / {
    #         proxy_pass http://whatsapp-bot;
    #         proxy_http_version 1.1;
    #         proxy_set_header Upgrade $http_upgrade;
    #         proxy_set_header Connection 'upgrade';
    #         proxy_set_header Host $host;
    #         proxy_cache_bypass $http_upgrade;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto $scheme;
    #     }
    # }
}